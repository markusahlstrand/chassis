// FSM demo vertical (ServiceCo). Implementation follows ../spec/testrun.md;
// blocked on the kernel deltas in §9.2 of that spec. This placeholder keeps the
// package compiling and the workspace shape visible.
export const DEMO = 'fsm' as const;
