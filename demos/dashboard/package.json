{
  "name": "@substrat-run/demo-dashboard",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "The Dashboard — the tenant-facing self-service platform vertical (docs/design/dashboard.md). M0 prototype.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@substrat-run/contracts": "workspace:^",
    "@substrat-run/kernel": "workspace:^",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@substrat-run/adapter-sqlite": "workspace:^",
    "@substrat-run/engine-protocol": "workspace:^",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
