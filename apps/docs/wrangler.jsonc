{
  // account_id is NOT supported in a Pages config file (wrangler validates it out);
  // it is supplied via the CLOUDFLARE_ACCOUNT_ID env var by the deploy script instead.
  "name": "substrat-docs",
  "pages_build_output_dir": ".vitepress/dist"
}
